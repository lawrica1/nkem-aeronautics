MONGODB_URI=mongodb://localhost:27017/nkem
JWT_SECRET=replace-with-a-long-random-string

# Optional — only needed if the API is served from a different origin
# than the frontend (defaults to same-origin "/api" otherwise).
# NEXT_PUBLIC_API_URL=
